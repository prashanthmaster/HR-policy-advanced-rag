# Slot 4 — HR-Policy Advanced RAG
#
# Model choices are budget-constrained, confirmed 2026-09-03 ($4.98 OpenAI
# balance): gpt-4o-mini (or gpt-5-nano if available) for all generation/
# grading/judge calls, text-embedding-3-small for all embeddings. Never the
# larger variants until a real measured cost says the budget allows it.
# See PROJECT_PLAN.md Phase 2 gate note and risk RK-4.
#
# Core pipeline
langchain>=0.3
langgraph>=0.2
langsmith
openai>=1.40

# Retrieval
rank-bm25
qdrant-client
flashrank

# Eval
ragas
datasets

# Google Drive freshness
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# API / serving
fastapi
uvicorn[standard]
pydantic>=2

# Utilities
python-dotenv
tiktoken
pypdf
beautifulsoup4
lxml

# Non-markdown source document parsers (ingestion/formats/) -- the corpus is
# authored in markdown for speed, but real HR documents arrive as PDF, Word,
# and Excel/CSV. These parsers are tested against real generated sample
# files under corpus_samples/multi_format/ (scripts/generate_multiformat_samples.py),
# not the scored 72-clause corpus itself, per the 3 Sep 2026 scope decision
# recorded in README.md and PROJECT_PLAN.md.
python-docx
openpyxl

# Dev/fixture-generation only -- writes the sample PDF above, never used at
# ingestion runtime (ingestion only ever READS pdfs, via pypdf).
fpdf2

# Testing / CI
pytest
pytest-asyncio
