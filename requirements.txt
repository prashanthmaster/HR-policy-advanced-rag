# Slot 4 — HR-Policy Advanced RAG
#
# Model choices are budget-constrained, confirmed 2026-09-03 ($4.98 OpenAI
# balance): gpt-4o-mini (or gpt-5-nano if available) for all generation/
# grading/judge calls, text-embedding-3-small for all embeddings. Never the
# larger variants until a real measured cost says the budget allows it.
# See PROJECT_PLAN.md Phase 2 gate note and risk RK-4.
#
# Core pipeline
#
# langchain/langchain-community/ragas pinned to EXACT versions, 2026-09-05
# (Phase 7 CI Change Log): a real GitHub Actions run on a clean Linux box
# resolved a different, incompatible combination than what's actually
# installed and working on Prashanth's own machine, and crashed deep
# inside langchain_community with "No module named
# 'langchain_community.chat_models.vertexai'" -- an optional integration
# neither this project nor ragas actually uses, pulled in only because an
# unpinned resolver picked a bad version mix. Pinning to the exact
# versions proven to work locally (captured via `pip freeze` on
# .venv-win) makes CI install the identical, already-working combination
# instead of "whatever's newest today."
langchain==1.3.18
langchain-community==0.4.2
langchain-core==1.6.1
langchain-openai==1.6.0
langchain-text-splitters==1.1.2
langgraph>=0.2
langsmith
openai==3.7.0

# Retrieval
rank-bm25
qdrant-client
flashrank

# Eval
ragas==0.3.1
datasets==5.0.1

# Google Drive freshness
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# API / serving
fastapi
uvicorn[standard]
pydantic>=2

# Utilities
python-dotenv
tiktoken
pypdf
beautifulsoup4
lxml

# Non-markdown source document parsers (ingestion/formats/) -- the corpus is
# authored in markdown for speed, but real HR documents arrive as PDF, Word,
# and Excel/CSV. These parsers are tested against real generated sample
# files under corpus_samples/multi_format/ (scripts/generate_multiformat_samples.py),
# not the scored 72-clause corpus itself, per the 3 Sep 2026 scope decision
# recorded in README.md and PROJECT_PLAN.md.
python-docx
openpyxl

# Dev/fixture-generation only -- writes the sample PDF above, never used at
# ingestion runtime (ingestion only ever READS pdfs, via pypdf).
fpdf2

# Testing / CI
pytest
pytest-asyncio
